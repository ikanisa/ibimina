# Progressive Web Apps

This directory contains the Staff Admin PWA surface. It is a fully independent
Next.js application wired into the pnpm workspace.

- `staff-admin/` – the operational console used by staff and administrators.

The app shares the workspace packages located in `../../packages`.
