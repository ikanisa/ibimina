# Cloudflare Pages configuration for Admin App
# Domain: adminsacco.ikanisa.com

name = "ibimina-admin"
compatibility_date = "2025-10-30"
pages_build_output_dir = ".vercel/output/static"

# Node.js compatibility for API routes and server-side code
compatibility_flags = ["nodejs_compat"]

# Build configuration
# Note: For Cloudflare Pages dashboard, use this build command:
# pnpm install --frozen-lockfile && pnpm build:packages && pnpm build:admin && npx @cloudflare/next-on-pages

# Note: Environment variables should be set in the Cloudflare dashboard under:
# Pages > [Project] > Settings > Build & deployments
# Pages > [Project] > Settings > Environment variables
