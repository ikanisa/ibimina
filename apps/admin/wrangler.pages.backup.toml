# Cloudflare Pages configuration for Admin/Staff App
# Domain: adminsacco.ikanisa.com

name = "ibimina-admin"
compatibility_date = "2025-10-30"
pages_build_output_dir = ".vercel/output/static"

# Node.js compatibility for API routes
compatibility_flags = ["nodejs_compat"]

# Note: For Cloudflare Pages, build configuration and environment variables
# should be set in the Cloudflare dashboard under:
# Pages > [Project] > Settings > Build & deployments
# Pages > [Project] > Settings > Environment variables
