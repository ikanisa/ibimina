<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Permit cleartext ONLY for these domains (for any stray http redirects/assets) -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">ibimina.rw</domain>
    <domain includeSubdomains="true">client.ibimina.rw</domain>
  </domain-config>

  <!-- Everything else follows standard system trust anchors over HTTPS -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>
</network-security-config>
