#!/bin/bash
# Skip hooks in CI environments to prevent failures when automated tools commit
if [[ -n "$CI" ]] || [[ "$GITHUB_ACTIONS" = "true" ]] || [[ "$HUSKY" = "0" ]]; then
  exit 0
fi

pnpm exec lint-staged
